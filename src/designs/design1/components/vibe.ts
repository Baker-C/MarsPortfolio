import type { ThemeVibe } from '../../../theme/themes'

/** Folio ornament glyph per layout vibe (footer, colophons, separators). */
export const vibeOrnament: Record<ThemeVibe, string> = {
  horizon: '❦',
  grove: '❧',
  pond: '❍',
  bloom: '✿',
  ridge: '▲',
  bouquet: '✾',
}
